import 'package:flutter/material.dart';
import 'package:gmeet/UI/Notes/components/notes_body.dart';

class Notes extends StatelessWidget {
  const Notes({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return NotesBody();
  }
}
